# ~/.profile: executed by the command interpreter for login shells.

{% if not log_shell_startup %}# {% endif %}{{ dotfiles_home }}/data/scripts/dotfiles_log.sh shell "shell/profile start [`pstree -s -p $$`]"

source "{{dotfiles_home}}/data/shell/environment.sh"
source "{{dotfiles_home}}/data/shell/rc.sh"

# if running bash
if [ -n "$BASH_VERSION" ]; then
    # include .bashrc if it exists
    if [ -f "$HOME/.bashrc" ]; then
	. "$HOME/.bashrc"
    fi
fi

{% if not log_shell_startup %}# {% endif %}{{ dotfiles_home }}/data/scripts/dotfiles_log.sh shell shell/profile end

